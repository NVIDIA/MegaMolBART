defaults:
  - megamolbart_pretrain_base

trainer:
  devices: 4
  num_nodes: 4

data:  
  decoder_augment: False

model:
  name: small_span
  # model architecture
  num_layers: 6
  hidden_size: 512
  num_attention_heads: 8
