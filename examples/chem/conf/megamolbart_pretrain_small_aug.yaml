defaults:
  - megamolbart_pretrain_base

trainer:
  devices: 4
  num_nodes: 4

data:  
  encoder_mask: False

model:
  name: small_aug
  # model architecture
  num_layers: 6
  hidden_size: 512
  num_attention_heads: 8
