metadata_file: 'metadata.txt'
dataset_type: 'zinc_csv'
micro_batch_size: 1
encoder_augment: False
encoder_mask: False
decoder_augment: False
decoder_mask: False
canonicalize_input: True # don't assume CSV data are canonicalized 
dataloader_type: 'single' # single = sequential is only option for now. cyclic = randomized
pin_memory: False # must be false for CSV dataset
drop_last: False
