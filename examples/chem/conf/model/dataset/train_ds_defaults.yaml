defaults:
  - dataset_defaults

encoder_augment: True # consistent with DEFAULT_AUGMENT in AZ script
encoder_mask: False
decoder_augment: False
canonicalize_input: False # consistent with setting in AZ script
batch_size: 32
num_workers: 10