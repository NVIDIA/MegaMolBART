defaults:
  - model_defaults
  - data: csv_data_defaults

# model architecture
seq_length: 512
max_position_embeddings: ${.seq_length}
num_layers: 2
hidden_size: 256
num_attention_heads: 4
ffn_hidden_size: ${multiply:4, ${.hidden_size}} # Transformer FFN hidden size. Usually 4 * hidden_size.
