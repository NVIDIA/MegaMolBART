defaults:
  - model_defaults_MP
  
name: xlarge
hidden_size: 1024 # the hidden_size
ffn_hidden_size: 4096 # Transformer FFN hidden size. Usually 4 * hidden_size.
num_attention_heads: 16
num_layers: 8
tensor_model_parallel_size: 1
 
